[phases.setup]
nixPkgs = ["python312"]
nixpkgsArchive = "ba913eda2df8eb72147259189d55932012df6301"

[phases.install]
cmds = ["cd back && pip install -r requirements.txt"]

[start]
cmd = "cd back && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
